package com.metamorph.delivery.delivery.entity;

public enum DeliveryStatus {
    PENDING,
    DRIVER_ASSIGNED,
    ARRIVED_PICKUP,
    PICKED_UP,
    EN_ROUTE,
    ARRIVED_DROPOFF,
    DELIVERED,
    FAILED
}
